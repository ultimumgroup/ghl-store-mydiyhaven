# v0.1.1: Scoped catalog requests and reuse terms

**Date**: 2026-09-09
**Author**: Codex
**Type**: Bug Fix
**Previous Version**: 0.1.0

Collections cold adapter time fell from 28.94 to 2.47 seconds in one live read-only comparison. The directory now shows collection cards and counts; product previews live on collection detail pages. Shared metadata and per-product price caches avoid unrelated variant reads on collection details, product details, home previews and saved carts.

| Files | Purpose |
| --- | --- |
| `src/lib/ghl.server.ts`, `ghl.functions.ts`, `catalog-query.ts` | Scoped reads, coalescing and caching |
| Collection routes, home content, cart context | Select only needed data |
| `tests/contracts.mjs`, `tests/collections-browser.py` | Request limits and browser regression coverage |
| `LICENSE`, `THIRD_PARTY_NOTICES.md`, `docs/OPEN-SOURCE.md`, README and screenshots | MIT grant for original contributions and explicit asset/upstream boundaries |
| `.env.example`, `src/lib/version.ts` | Safe setup placeholders and version tracking |
| `docs/PERFORMANCE.md` | Measurements, limitations and hosted follow-up |

No new public page routes, dependency updates, checkout enablement, GHL writes or framework changes. RPC additions must be transferred together with their callers and rebuilt in AI Studio. The previous handoff manifest is pinned to an older revision and does not include this change. The README screenshot was captured from the existing local storefront; no external donor code was added.

Validation: typecheck, contracts, build and all three browser scripts passed. Lint has zero errors and 11 existing warnings. Local validation verdict: SHIP for the catalog performance fix; this does not certify checkout or hosted timing.
