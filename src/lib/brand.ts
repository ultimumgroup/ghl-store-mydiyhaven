// Single source of truth for the storefront's brand identity.
export const BRAND_NAME = "My DIY Haven";

// Logo icon asset (icon only — the wordmark is rendered as text alongside it).
export const LOGO_URL = "/images/my-diy-haven-gold-icon.png";

// Order-number prefix used on the confirmation page.
export const ORDER_PREFIX = "MDH";

// Configured sub-account location identifier
